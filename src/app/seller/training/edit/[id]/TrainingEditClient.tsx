"use client";

import { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { toast } from 'react-hot-toast';
import { API_CONFIG } from '@/utils/config';

interface TrainingEditClientProps {
  params: { id: string };
}

const TrainingEditClient = ({ params }: TrainingEditClientProps) => {
  // Copier ici la logique du composant original
  return (
    <div>
      {/* Copier ici le JSX du composant original */}
    </div>
  );
};

export default TrainingEditClient; 